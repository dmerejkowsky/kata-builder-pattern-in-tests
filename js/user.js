class User {
    constructor({ name, age, verified, address }) {
        this.name = name
        this.age = age
        this.verified = verified
        this.address = address
    }
}

module.exports = User